# TODO: Need to document the ServiceAccount needs. OBI requires to setup a service account
# and roles to watch and get information about Nodes, Services, ReplicaSets and Pods
apiVersion: v1
kind: ServiceAccount
metadata:
  name: obi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: obi
rules:
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["list", "watch"]
  - apiGroups: [""]
    resources: ["services", "pods", "nodes"]
    verbs: ["list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: obi
subjects:
  - kind: ServiceAccount
    name: obi
    namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: obi
---
apiVersion: v1
kind: Service
metadata:
  name: testserver
spec:
  selector:
    app: testserver
  ports:
    - port: 8080
      name: http0
      targetPort: http0
    - port: 8081
      name: http1
      targetPort: http1
    - port: 8082
      name: http2
      targetPort: http2
    - port: 8083
      name: http
      targetPort: http3
    - port: 5051
      name: grpc
      targetPort: grpc
---
apiVersion: v1
kind: Pod
metadata:
  name: testserver
  labels:
    app: testserver
    # this label will trigger a deletion of OBI pods before tearing down
    # kind, to force OBI writing the coverage data
    teardown: delete
    deployment.environment.name: test
    app.kubernetes.io/service.version: 3.2.1
  annotations:
    resource.opentelemetry.io/deployment.environment.name: 'development'
spec:
  shareProcessNamespace: true
  serviceAccountName: obi
  volumes:
    - name: configs
      persistentVolumeClaim:
        claimName: configs
    - name: testoutput
      persistentVolumeClaim:
        claimName: testoutput
  containers:
    - name: testserver
      image: pythontestserver:dev
      imagePullPolicy: Never # loaded into Kind from localhost
      ports:
        # exposing hostports to enable operation from tests
        - containerPort: 8080
          hostPort: 8080
          name: http0
      env:
        - name: SERVICE_VERSION_ID
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.labels['app.kubernetes.io/service.version']
        - name: LOG_LEVEL
          value: "DEBUG"
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "service.name=mypythonapp,service.version=$(SERVICE_VERSION_ID)"
    - name: obi
      image: obi:dev
      imagePullPolicy: Never # loaded into Kind from localhost
      securityContext:
        privileged: true
      args: ["--config=/configs/obi-config.yml"]
      volumeMounts:
        - mountPath: /configs
          name: configs
        - mountPath: /testoutput
          name: testoutput
      env:
        - name: GOCOVERDIR
          value: "/testoutput"
        - name: OTEL_EBPF_TRACE_PRINTER
          value: "json"
        - name: OTEL_EBPF_OPEN_PORT
          value: "8080"
        - name: OTEL_EBPF_DISCOVERY_POLL_INTERVAL
          value: "500ms"
        - name: OTEL_EBPF_SERVICE_NAMESPACE
          value: "integration-test"
        - name: OTEL_EBPF_METRICS_INTERVAL
          value: "1s"
        - name: OTEL_EBPF_BPF_BATCH_TIMEOUT
          value: "10ms"
        - name: OTEL_EBPF_LOG_LEVEL
          value: "DEBUG"
        - name: OTEL_EBPF_BPF_DEBUG
          value: "TRUE"
        - name: OTEL_EBPF_INTERNAL_METRICS_PROMETHEUS_PORT
          value: "8999"
        - name: OTEL_EBPF_KUBE_METADATA_ENABLE
          value: "autodetect"
        - name: OTEL_EBPF_NAME_RESOLVER_SOURCES
          value: "dns,k8s"
